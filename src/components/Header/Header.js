'use strict';

import React from 'react';
import Navbar from '../Navbar';

export default React.createClass({

    render: function() {
        return (
          <header>
            <Navbar/>
         </header>
        );
    }
});
